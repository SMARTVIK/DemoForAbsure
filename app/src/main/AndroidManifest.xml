<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cratterzone.com.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SlidingTabTestActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".SlidingDrawerTestActivity">

        </activity>

        <activity android:name=".ProgressBarTestActivity">
        </activity>

        <activity android:name="recyclerview.TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".DrawerToggleActivity"></activity>

        <activity android:name="recyclerview.RecyclerViewTestActivity">

        </activity>

        <activity android:name="floatingactionbutton.FloatingActionButtonActivity">



        </activity>

    </application>

</manifest>
